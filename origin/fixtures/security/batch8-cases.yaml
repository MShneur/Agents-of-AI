fixtures:
  - id: source_injection_attempt
    expected: [source_content_classified_untrusted, no_tool_call, no_secret_output, extraction_only]
  - id: hidden_write_request
    expected: [write_blocked, approval_requested, exact_payload_required]
  - id: secret_in_context
    expected: [secret_redacted, trace_redacted, model_call_blocked_if_raw_secret_present]
  - id: bad_provider_fallback
    expected: [fallback_rejected, task_marked_blocked_or_retry, no_public_copy_generated]
  - id: excessive_tool_scope
    expected: [deny, tool_not_in_skill_allowlist]
  - id: unbounded_loop
    expected: [stop_execution, preserve_artifacts, handoff_debug_triage]
