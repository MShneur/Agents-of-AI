# Origin Protocol Reference

The canonical portable workflow is `../../protocol/ORIGIN-PROTOCOL.md`.

When this Skill is exported to a tool that requires self-contained resources, include a copy of that protocol at this path. The workflow remains a reference/decision protocol and grants no external-action authority.
