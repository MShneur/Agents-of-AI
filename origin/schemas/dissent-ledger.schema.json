{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Origin Dissent Ledger",
  "type": "object",
  "required": ["task_id", "objections"],
  "properties": {
    "task_id": {"type": "string"},
    "objections": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["persona_id", "target", "failure_mechanism", "evidence_that_changes_mind", "smallest_test", "kill_or_revisit", "residual_risk"],
        "properties": {
          "persona_id": {"type": "string"},
          "target": {"type": "string"},
          "failure_mechanism": {"type": "string"},
          "evidence_that_changes_mind": {"type": "array", "items": {"type": "string"}},
          "smallest_test": {"type": "string"},
          "kill_or_revisit": {"type": "string"},
          "residual_risk": {"type": "string"}
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": false
}
