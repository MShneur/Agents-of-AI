# Alt-ssembly Required — Development A Implementation Record

Status: **DEVELOPMENT A COMPLETE AT ENTRY LEVEL; PRE-MERGE ROSTER SYNC DEFERRED TO DEVELOPMENT B**

## Built

- `agents/alt-ssembly-required.md`
- `workflows/alt-ssembly.md`
- `CHANGELOG.md` entry for the new agent/workflow and resulting live-directory counts

## Agent contract

Alt-ssembly Required is now encoded as the accountable whole-product assembly operator rather than a monolithic coding framework.

It establishes four scalable contracts:

1. Product Packet
2. Runtime Envelope
3. Novice Contract
4. Evidence Contract

It derives a Preservation Ledger before implementation and routes existing Agents-of-AI methods for implementation, diagnosis, research, repo maintenance, security, verification, loop cleaning, expert review, and human gates.

## Workflow contract

The reusable `alt-ssembly` workflow has eight stages:

```text
RECONCILE
-> PRODUCT MAP + PRESERVATION LEDGER
-> RUNTIME + HUMAN ENVELOPE
-> EVIDENCE CONTRACT + BUILD PLAN
-> BUILD / ROUTE
-> INTEGRATE + REGRESSION
-> FIELD / CANARY
-> ASSEMBLY RECEIPT
```

A Lite Variant is included for small reversible work so the process scales down.

## Safety / anti-overcorrection rules encoded

- Working accepted surfaces survive by default.
- Working-feature deletion requires explicit Product Packet `KILL` plus Human Gate where consequential.
- Same-file implementation does not merge product-layer authority.
- Different browsers/runtimes/hosts are not treated as equivalent without evidence.
- Synthetic evidence does not become field proof.
- Novice simplification favors progressive disclosure before capability deletion.
- Named practitioners are sourced live through Quorum from public work; no simulated endorsements.
- Cleanerz auto-fires on repair loops, feature-deletion-by-simplification, scope spread, duplicate architecture, and evidence-class inflation.
- Single Dispatch Operator prevents recursive manager sprawl and overlapping writers.

## Documentation state

`CHANGELOG.md` records the new entries and the resulting directory-level roster if merged:

- 23 personas
- 14 agents
- 17 workflows
- 17 techniques
- 4 modes
- 6 teams
- 6 failures
- **86 total composable entries**

The root `README.md` and generated quick/research roster prompts have not been rewritten in Development A because their repository maintenance flow already uses `scripts/sync-roster.py`, and this connected editing surface only offers full-file replacement for those long generated/discovery documents. Development B must run the canonical roster sync before merge rather than hand-maintaining a second source of truth.

This is an intentional preservation decision, not a completed-publication claim.

## Development B gate

Development B must now:

1. red-team Alt-ssembly against the Ghost in the Loop historical failure set;
2. run a Cleanerz simplification pass on Alt-ssembly itself;
3. fix any duplicated responsibilities or ceremonial process;
4. create the portable ChatGPT/skill activation artifact;
5. create the Ghost-specific activation prompt without hardcoding Ghost behavior into the universal agent;
6. run the canonical roster/documentation synchronization and verify counts;
7. prepare the branch for human review/merge.

## Claim ceiling

Current justified claim:

> **The canonical AoA agent and reusable workflow have been authored on the research branch and structurally checked against the repository schemas. They have not yet completed the Development B adversarial/portable-install/pre-merge synchronization pass.**
